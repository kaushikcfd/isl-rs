// Automatically generated by isl_bindings_generator.
// LICENSE: MIT

use super::{ASTExpr, Context, IdList, IdToASTExpr};
use libc::uintptr_t;
use std::ffi::{CStr, CString};
use std::os::raw::c_char;

/// Wraps `isl_id`.
pub struct Id {
    pub ptr: uintptr_t,
    pub should_free_on_drop: bool,
}

extern "C" {

    fn isl_id_copy(id: uintptr_t) -> uintptr_t;

    fn isl_id_dump(id: uintptr_t) -> ();

    fn isl_id_free(id: uintptr_t) -> uintptr_t;

    fn isl_id_get_ctx(id: uintptr_t) -> uintptr_t;

    fn isl_id_get_free_user(id: uintptr_t) -> ();

    fn isl_id_get_hash(id: uintptr_t) -> u32;

    fn isl_id_get_name(id: uintptr_t) -> *const c_char;

    fn isl_id_list_add(list: uintptr_t, el: uintptr_t) -> uintptr_t;

    fn isl_id_list_alloc(ctx: uintptr_t, n: i32) -> uintptr_t;

    fn isl_id_list_clear(list: uintptr_t) -> uintptr_t;

    fn isl_id_list_concat(list1: uintptr_t, list2: uintptr_t) -> uintptr_t;

    fn isl_id_list_copy(list: uintptr_t) -> uintptr_t;

    fn isl_id_list_drop(list: uintptr_t, first: u32, n: u32) -> uintptr_t;

    fn isl_id_list_dump(list: uintptr_t) -> ();

    fn isl_id_list_free(list: uintptr_t) -> uintptr_t;

    fn isl_id_list_from_id(el: uintptr_t) -> uintptr_t;

    fn isl_id_list_get_at(list: uintptr_t, index: i32) -> uintptr_t;

    fn isl_id_list_get_ctx(list: uintptr_t) -> uintptr_t;

    fn isl_id_list_get_id(list: uintptr_t, index: i32) -> uintptr_t;

    fn isl_id_list_insert(list: uintptr_t, pos: u32, el: uintptr_t) -> uintptr_t;

    fn isl_id_list_n_id(list: uintptr_t) -> i32;

    fn isl_id_list_read_from_str(ctx: uintptr_t, str_: *const c_char) -> uintptr_t;

    fn isl_id_list_reverse(list: uintptr_t) -> uintptr_t;

    fn isl_id_list_set_at(list: uintptr_t, index: i32, el: uintptr_t) -> uintptr_t;

    fn isl_id_list_set_id(list: uintptr_t, index: i32, el: uintptr_t) -> uintptr_t;

    fn isl_id_list_size(list: uintptr_t) -> i32;

    fn isl_id_list_swap(list: uintptr_t, pos1: u32, pos2: u32) -> uintptr_t;

    fn isl_id_list_to_str(list: uintptr_t) -> *const c_char;

    fn isl_id_read_from_str(ctx: uintptr_t, str_: *const c_char) -> uintptr_t;

    fn isl_id_to_ast_expr_alloc(ctx: uintptr_t, min_size: i32) -> uintptr_t;

    fn isl_id_to_ast_expr_copy(hmap: uintptr_t) -> uintptr_t;

    fn isl_id_to_ast_expr_drop(hmap: uintptr_t, key: uintptr_t) -> uintptr_t;

    fn isl_id_to_ast_expr_dump(hmap: uintptr_t) -> ();

    fn isl_id_to_ast_expr_free(hmap: uintptr_t) -> uintptr_t;

    fn isl_id_to_ast_expr_get(hmap: uintptr_t, key: uintptr_t) -> uintptr_t;

    fn isl_id_to_ast_expr_get_ctx(hmap: uintptr_t) -> uintptr_t;

    fn isl_id_to_ast_expr_has(hmap: uintptr_t, key: uintptr_t) -> i32;

    fn isl_id_to_ast_expr_is_equal(hmap1: uintptr_t, hmap2: uintptr_t) -> i32;

    fn isl_id_to_ast_expr_read_from_str(ctx: uintptr_t, str_: *const c_char) -> uintptr_t;

    fn isl_id_to_ast_expr_set(hmap: uintptr_t, key: uintptr_t, val: uintptr_t) -> uintptr_t;

    fn isl_id_to_ast_expr_to_str(hmap: uintptr_t) -> *const c_char;

    fn isl_id_to_list(el: uintptr_t) -> uintptr_t;

    fn isl_id_to_str(id: uintptr_t) -> *const c_char;

}

impl Id {
    /// Wraps `isl_id_copy`.
    pub fn copy(&self) -> Id {
        let id = self;
        let id = id.ptr;
        let isl_rs_result = unsafe { isl_id_copy(id) };
        let isl_rs_result = Id { ptr: isl_rs_result,
                                 should_free_on_drop: true };
        isl_rs_result
    }

    /// Wraps `isl_id_dump`.
    pub fn dump(&self) -> () {
        let id = self;
        let id = id.ptr;
        let isl_rs_result = unsafe { isl_id_dump(id) };
        isl_rs_result
    }

    /// Wraps `isl_id_free`.
    pub fn free(self) -> Id {
        let id = self;
        let mut id = id;
        id.do_not_free_on_drop();
        let id = id.ptr;
        let isl_rs_result = unsafe { isl_id_free(id) };
        let isl_rs_result = Id { ptr: isl_rs_result,
                                 should_free_on_drop: true };
        isl_rs_result
    }

    /// Wraps `isl_id_get_ctx`.
    pub fn get_ctx(&self) -> Context {
        let id = self;
        let id = id.ptr;
        let isl_rs_result = unsafe { isl_id_get_ctx(id) };
        let isl_rs_result = Context { ptr: isl_rs_result,
                                      should_free_on_drop: false };
        isl_rs_result
    }

    /// Wraps `isl_id_get_free_user`.
    pub fn get_free_user(&self) -> () {
        let id = self;
        let id = id.ptr;
        let isl_rs_result = unsafe { isl_id_get_free_user(id) };
        isl_rs_result
    }

    /// Wraps `isl_id_get_hash`.
    pub fn get_hash(&self) -> u32 {
        let id = self;
        let id = id.ptr;
        let isl_rs_result = unsafe { isl_id_get_hash(id) };
        isl_rs_result
    }

    /// Wraps `isl_id_get_name`.
    pub fn get_name(&self) -> &str {
        let id = self;
        let id = id.ptr;
        let isl_rs_result = unsafe { isl_id_get_name(id) };
        let isl_rs_result = unsafe { CStr::from_ptr(isl_rs_result) };
        let isl_rs_result = isl_rs_result.to_str().unwrap();
        isl_rs_result
    }

    /// Wraps `isl_id_list_add`.
    pub fn list_add(list: IdList, el: Id) -> IdList {
        let mut list = list;
        list.do_not_free_on_drop();
        let list = list.ptr;
        let mut el = el;
        el.do_not_free_on_drop();
        let el = el.ptr;
        let isl_rs_result = unsafe { isl_id_list_add(list, el) };
        let isl_rs_result = IdList { ptr: isl_rs_result,
                                     should_free_on_drop: true };
        isl_rs_result
    }

    /// Wraps `isl_id_list_alloc`.
    pub fn list_alloc(ctx: &Context, n: i32) -> IdList {
        let ctx = ctx.ptr;
        let isl_rs_result = unsafe { isl_id_list_alloc(ctx, n) };
        let isl_rs_result = IdList { ptr: isl_rs_result,
                                     should_free_on_drop: true };
        isl_rs_result
    }

    /// Wraps `isl_id_list_clear`.
    pub fn list_clear(list: IdList) -> IdList {
        let mut list = list;
        list.do_not_free_on_drop();
        let list = list.ptr;
        let isl_rs_result = unsafe { isl_id_list_clear(list) };
        let isl_rs_result = IdList { ptr: isl_rs_result,
                                     should_free_on_drop: true };
        isl_rs_result
    }

    /// Wraps `isl_id_list_concat`.
    pub fn list_concat(list1: IdList, list2: IdList) -> IdList {
        let mut list1 = list1;
        list1.do_not_free_on_drop();
        let list1 = list1.ptr;
        let mut list2 = list2;
        list2.do_not_free_on_drop();
        let list2 = list2.ptr;
        let isl_rs_result = unsafe { isl_id_list_concat(list1, list2) };
        let isl_rs_result = IdList { ptr: isl_rs_result,
                                     should_free_on_drop: true };
        isl_rs_result
    }

    /// Wraps `isl_id_list_copy`.
    pub fn list_copy(list: &IdList) -> IdList {
        let list = list.ptr;
        let isl_rs_result = unsafe { isl_id_list_copy(list) };
        let isl_rs_result = IdList { ptr: isl_rs_result,
                                     should_free_on_drop: true };
        isl_rs_result
    }

    /// Wraps `isl_id_list_drop`.
    pub fn list_drop(list: IdList, first: u32, n: u32) -> IdList {
        let mut list = list;
        list.do_not_free_on_drop();
        let list = list.ptr;
        let isl_rs_result = unsafe { isl_id_list_drop(list, first, n) };
        let isl_rs_result = IdList { ptr: isl_rs_result,
                                     should_free_on_drop: true };
        isl_rs_result
    }

    /// Wraps `isl_id_list_dump`.
    pub fn list_dump(list: &IdList) -> () {
        let list = list.ptr;
        let isl_rs_result = unsafe { isl_id_list_dump(list) };
        isl_rs_result
    }

    /// Wraps `isl_id_list_free`.
    pub fn list_free(list: IdList) -> IdList {
        let mut list = list;
        list.do_not_free_on_drop();
        let list = list.ptr;
        let isl_rs_result = unsafe { isl_id_list_free(list) };
        let isl_rs_result = IdList { ptr: isl_rs_result,
                                     should_free_on_drop: true };
        isl_rs_result
    }

    /// Wraps `isl_id_list_from_id`.
    pub fn list_from_id(self) -> IdList {
        let el = self;
        let mut el = el;
        el.do_not_free_on_drop();
        let el = el.ptr;
        let isl_rs_result = unsafe { isl_id_list_from_id(el) };
        let isl_rs_result = IdList { ptr: isl_rs_result,
                                     should_free_on_drop: true };
        isl_rs_result
    }

    /// Wraps `isl_id_list_get_at`.
    pub fn list_get_at(list: &IdList, index: i32) -> Id {
        let list = list.ptr;
        let isl_rs_result = unsafe { isl_id_list_get_at(list, index) };
        let isl_rs_result = Id { ptr: isl_rs_result,
                                 should_free_on_drop: true };
        isl_rs_result
    }

    /// Wraps `isl_id_list_get_ctx`.
    pub fn list_get_ctx(list: &IdList) -> Context {
        let list = list.ptr;
        let isl_rs_result = unsafe { isl_id_list_get_ctx(list) };
        let isl_rs_result = Context { ptr: isl_rs_result,
                                      should_free_on_drop: false };
        isl_rs_result
    }

    /// Wraps `isl_id_list_get_id`.
    pub fn list_get_id(list: &IdList, index: i32) -> Id {
        let list = list.ptr;
        let isl_rs_result = unsafe { isl_id_list_get_id(list, index) };
        let isl_rs_result = Id { ptr: isl_rs_result,
                                 should_free_on_drop: true };
        isl_rs_result
    }

    /// Wraps `isl_id_list_insert`.
    pub fn list_insert(list: IdList, pos: u32, el: Id) -> IdList {
        let mut list = list;
        list.do_not_free_on_drop();
        let list = list.ptr;
        let mut el = el;
        el.do_not_free_on_drop();
        let el = el.ptr;
        let isl_rs_result = unsafe { isl_id_list_insert(list, pos, el) };
        let isl_rs_result = IdList { ptr: isl_rs_result,
                                     should_free_on_drop: true };
        isl_rs_result
    }

    /// Wraps `isl_id_list_n_id`.
    pub fn list_n_id(list: &IdList) -> i32 {
        let list = list.ptr;
        let isl_rs_result = unsafe { isl_id_list_n_id(list) };
        isl_rs_result
    }

    /// Wraps `isl_id_list_read_from_str`.
    pub fn list_read_from_str(ctx: &Context, str_: &str) -> IdList {
        let ctx = ctx.ptr;
        let str_ = CString::new(str_).unwrap();
        let str_ = str_.as_ptr();
        let isl_rs_result = unsafe { isl_id_list_read_from_str(ctx, str_) };
        let isl_rs_result = IdList { ptr: isl_rs_result,
                                     should_free_on_drop: true };
        isl_rs_result
    }

    /// Wraps `isl_id_list_reverse`.
    pub fn list_reverse(list: IdList) -> IdList {
        let mut list = list;
        list.do_not_free_on_drop();
        let list = list.ptr;
        let isl_rs_result = unsafe { isl_id_list_reverse(list) };
        let isl_rs_result = IdList { ptr: isl_rs_result,
                                     should_free_on_drop: true };
        isl_rs_result
    }

    /// Wraps `isl_id_list_set_at`.
    pub fn list_set_at(list: IdList, index: i32, el: Id) -> IdList {
        let mut list = list;
        list.do_not_free_on_drop();
        let list = list.ptr;
        let mut el = el;
        el.do_not_free_on_drop();
        let el = el.ptr;
        let isl_rs_result = unsafe { isl_id_list_set_at(list, index, el) };
        let isl_rs_result = IdList { ptr: isl_rs_result,
                                     should_free_on_drop: true };
        isl_rs_result
    }

    /// Wraps `isl_id_list_set_id`.
    pub fn list_set_id(list: IdList, index: i32, el: Id) -> IdList {
        let mut list = list;
        list.do_not_free_on_drop();
        let list = list.ptr;
        let mut el = el;
        el.do_not_free_on_drop();
        let el = el.ptr;
        let isl_rs_result = unsafe { isl_id_list_set_id(list, index, el) };
        let isl_rs_result = IdList { ptr: isl_rs_result,
                                     should_free_on_drop: true };
        isl_rs_result
    }

    /// Wraps `isl_id_list_size`.
    pub fn list_size(list: &IdList) -> i32 {
        let list = list.ptr;
        let isl_rs_result = unsafe { isl_id_list_size(list) };
        isl_rs_result
    }

    /// Wraps `isl_id_list_swap`.
    pub fn list_swap(list: IdList, pos1: u32, pos2: u32) -> IdList {
        let mut list = list;
        list.do_not_free_on_drop();
        let list = list.ptr;
        let isl_rs_result = unsafe { isl_id_list_swap(list, pos1, pos2) };
        let isl_rs_result = IdList { ptr: isl_rs_result,
                                     should_free_on_drop: true };
        isl_rs_result
    }

    /// Wraps `isl_id_list_to_str`.
    pub fn list_to_str(list: &IdList) -> &str {
        let list = list.ptr;
        let isl_rs_result = unsafe { isl_id_list_to_str(list) };
        let isl_rs_result = unsafe { CStr::from_ptr(isl_rs_result) };
        let isl_rs_result = isl_rs_result.to_str().unwrap();
        isl_rs_result
    }

    /// Wraps `isl_id_read_from_str`.
    pub fn read_from_str(ctx: &Context, str_: &str) -> Id {
        let ctx = ctx.ptr;
        let str_ = CString::new(str_).unwrap();
        let str_ = str_.as_ptr();
        let isl_rs_result = unsafe { isl_id_read_from_str(ctx, str_) };
        let isl_rs_result = Id { ptr: isl_rs_result,
                                 should_free_on_drop: true };
        isl_rs_result
    }

    /// Wraps `isl_id_to_ast_expr_alloc`.
    pub fn to_ast_expr_alloc(ctx: &Context, min_size: i32) -> IdToASTExpr {
        let ctx = ctx.ptr;
        let isl_rs_result = unsafe { isl_id_to_ast_expr_alloc(ctx, min_size) };
        let isl_rs_result = IdToASTExpr { ptr: isl_rs_result,
                                          should_free_on_drop: true };
        isl_rs_result
    }

    /// Wraps `isl_id_to_ast_expr_copy`.
    pub fn to_ast_expr_copy(hmap: &IdToASTExpr) -> IdToASTExpr {
        let hmap = hmap.ptr;
        let isl_rs_result = unsafe { isl_id_to_ast_expr_copy(hmap) };
        let isl_rs_result = IdToASTExpr { ptr: isl_rs_result,
                                          should_free_on_drop: true };
        isl_rs_result
    }

    /// Wraps `isl_id_to_ast_expr_drop`.
    pub fn to_ast_expr_drop(hmap: IdToASTExpr, key: Id) -> IdToASTExpr {
        let mut hmap = hmap;
        hmap.do_not_free_on_drop();
        let hmap = hmap.ptr;
        let mut key = key;
        key.do_not_free_on_drop();
        let key = key.ptr;
        let isl_rs_result = unsafe { isl_id_to_ast_expr_drop(hmap, key) };
        let isl_rs_result = IdToASTExpr { ptr: isl_rs_result,
                                          should_free_on_drop: true };
        isl_rs_result
    }

    /// Wraps `isl_id_to_ast_expr_dump`.
    pub fn to_ast_expr_dump(hmap: &IdToASTExpr) -> () {
        let hmap = hmap.ptr;
        let isl_rs_result = unsafe { isl_id_to_ast_expr_dump(hmap) };
        isl_rs_result
    }

    /// Wraps `isl_id_to_ast_expr_free`.
    pub fn to_ast_expr_free(hmap: IdToASTExpr) -> IdToASTExpr {
        let mut hmap = hmap;
        hmap.do_not_free_on_drop();
        let hmap = hmap.ptr;
        let isl_rs_result = unsafe { isl_id_to_ast_expr_free(hmap) };
        let isl_rs_result = IdToASTExpr { ptr: isl_rs_result,
                                          should_free_on_drop: true };
        isl_rs_result
    }

    /// Wraps `isl_id_to_ast_expr_get`.
    pub fn to_ast_expr_get(hmap: &IdToASTExpr, key: Id) -> ASTExpr {
        let hmap = hmap.ptr;
        let mut key = key;
        key.do_not_free_on_drop();
        let key = key.ptr;
        let isl_rs_result = unsafe { isl_id_to_ast_expr_get(hmap, key) };
        let isl_rs_result = ASTExpr { ptr: isl_rs_result,
                                      should_free_on_drop: true };
        isl_rs_result
    }

    /// Wraps `isl_id_to_ast_expr_get_ctx`.
    pub fn to_ast_expr_get_ctx(hmap: &IdToASTExpr) -> Context {
        let hmap = hmap.ptr;
        let isl_rs_result = unsafe { isl_id_to_ast_expr_get_ctx(hmap) };
        let isl_rs_result = Context { ptr: isl_rs_result,
                                      should_free_on_drop: false };
        isl_rs_result
    }

    /// Wraps `isl_id_to_ast_expr_has`.
    pub fn to_ast_expr_has(hmap: &IdToASTExpr, key: &Id) -> bool {
        let hmap = hmap.ptr;
        let key = key.ptr;
        let isl_rs_result = unsafe { isl_id_to_ast_expr_has(hmap, key) };
        let isl_rs_result = match isl_rs_result {
            0 => false,
            1 => true,
            _ => panic!("Got isl_bool = -1"),
        };
        isl_rs_result
    }

    /// Wraps `isl_id_to_ast_expr_is_equal`.
    pub fn to_ast_expr_is_equal(hmap1: &IdToASTExpr, hmap2: &IdToASTExpr) -> bool {
        let hmap1 = hmap1.ptr;
        let hmap2 = hmap2.ptr;
        let isl_rs_result = unsafe { isl_id_to_ast_expr_is_equal(hmap1, hmap2) };
        let isl_rs_result = match isl_rs_result {
            0 => false,
            1 => true,
            _ => panic!("Got isl_bool = -1"),
        };
        isl_rs_result
    }

    /// Wraps `isl_id_to_ast_expr_read_from_str`.
    pub fn to_ast_expr_read_from_str(ctx: &Context, str_: &str) -> IdToASTExpr {
        let ctx = ctx.ptr;
        let str_ = CString::new(str_).unwrap();
        let str_ = str_.as_ptr();
        let isl_rs_result = unsafe { isl_id_to_ast_expr_read_from_str(ctx, str_) };
        let isl_rs_result = IdToASTExpr { ptr: isl_rs_result,
                                          should_free_on_drop: true };
        isl_rs_result
    }

    /// Wraps `isl_id_to_ast_expr_set`.
    pub fn to_ast_expr_set(hmap: IdToASTExpr, key: Id, val: ASTExpr) -> IdToASTExpr {
        let mut hmap = hmap;
        hmap.do_not_free_on_drop();
        let hmap = hmap.ptr;
        let mut key = key;
        key.do_not_free_on_drop();
        let key = key.ptr;
        let mut val = val;
        val.do_not_free_on_drop();
        let val = val.ptr;
        let isl_rs_result = unsafe { isl_id_to_ast_expr_set(hmap, key, val) };
        let isl_rs_result = IdToASTExpr { ptr: isl_rs_result,
                                          should_free_on_drop: true };
        isl_rs_result
    }

    /// Wraps `isl_id_to_ast_expr_to_str`.
    pub fn to_ast_expr_to_str(hmap: &IdToASTExpr) -> &str {
        let hmap = hmap.ptr;
        let isl_rs_result = unsafe { isl_id_to_ast_expr_to_str(hmap) };
        let isl_rs_result = unsafe { CStr::from_ptr(isl_rs_result) };
        let isl_rs_result = isl_rs_result.to_str().unwrap();
        isl_rs_result
    }

    /// Wraps `isl_id_to_list`.
    pub fn to_list(self) -> IdList {
        let el = self;
        let mut el = el;
        el.do_not_free_on_drop();
        let el = el.ptr;
        let isl_rs_result = unsafe { isl_id_to_list(el) };
        let isl_rs_result = IdList { ptr: isl_rs_result,
                                     should_free_on_drop: true };
        isl_rs_result
    }

    /// Wraps `isl_id_to_str`.
    pub fn to_str(&self) -> &str {
        let id = self;
        let id = id.ptr;
        let isl_rs_result = unsafe { isl_id_to_str(id) };
        let isl_rs_result = unsafe { CStr::from_ptr(isl_rs_result) };
        let isl_rs_result = isl_rs_result.to_str().unwrap();
        isl_rs_result
    }

    /// Does not call isl_id_free() on being dropped. (For internal use only.)
    pub fn do_not_free_on_drop(&mut self) {
        self.should_free_on_drop = false;
    }
}

impl Drop for Id {
    fn drop(&mut self) {
        if self.should_free_on_drop {
            unsafe {
                isl_id_free(self.ptr);
            }
        }
    }
}
