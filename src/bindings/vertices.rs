// Automatically generated by isl_bindings_generator.
// LICENSE: MIT

use super::Context;
use libc::uintptr_t;

/// Wraps `isl_vertices`.
pub struct Vertices {
    pub ptr: uintptr_t,
    pub should_free_on_drop: bool,
}

extern "C" {

    fn isl_vertices_free(vertices: uintptr_t) -> uintptr_t;

    fn isl_vertices_get_ctx(vertices: uintptr_t) -> uintptr_t;

    fn isl_vertices_get_n_vertices(vertices: uintptr_t) -> i32;

}

impl Vertices {
    /// Wraps `isl_vertices_free`.
    pub fn free(self) -> Vertices {
        let vertices = self;
        let mut vertices = vertices;
        vertices.do_not_free_on_drop();
        let vertices = vertices.ptr;
        let isl_rs_result = unsafe { isl_vertices_free(vertices) };
        let isl_rs_result = Vertices { ptr: isl_rs_result,
                                       should_free_on_drop: true };
        isl_rs_result
    }

    /// Wraps `isl_vertices_get_ctx`.
    pub fn get_ctx(&self) -> Context {
        let vertices = self;
        let vertices = vertices.ptr;
        let isl_rs_result = unsafe { isl_vertices_get_ctx(vertices) };
        let isl_rs_result = Context { ptr: isl_rs_result,
                                      should_free_on_drop: false };
        isl_rs_result
    }

    /// Wraps `isl_vertices_get_n_vertices`.
    pub fn get_n_vertices(&self) -> i32 {
        let vertices = self;
        let vertices = vertices.ptr;
        let isl_rs_result = unsafe { isl_vertices_get_n_vertices(vertices) };
        isl_rs_result
    }

    /// Does not call isl_vertices_free() on being dropped. (For internal use
    /// only.)
    pub fn do_not_free_on_drop(&mut self) {
        self.should_free_on_drop = false;
    }
}

impl Drop for Vertices {
    fn drop(&mut self) {
        if self.should_free_on_drop {
            unsafe {
                isl_vertices_free(self.ptr);
            }
        }
    }
}
